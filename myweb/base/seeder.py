# from .models import Group, Level
#
# def seeder_func():
#     groups = ['Graphic Design', 'Cyber Security', 'Data Science', 'Machine Learning']
#     # levels = ['Junior', 'mid-level', 'Senior', 'Leader']
#
#     for group in groups:
#         if Group.objects.filter(name=group):
#             new_group = Group(name=group)
#             new_group.save()
#
#
#     # for level in levels:
#     #     if Level.objects.filter(level=level):
#     #         new_level = Group(level=level)
#     #         new_level.save()